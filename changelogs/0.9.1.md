# v0.9.1 - 2026-03-22

## 版本概览

本次版本聚焦两件事：
1. 修复测试数据污染生产库的问题（pytest 使用独立测试数据库）。
2. 清理历史脏数据，仅保留指定账户 `team1 / 一只长角的鸟`。

## 主要变更

1. 测试数据库隔离
   - 调整 `tests/conftest.py`，在导入应用前强制注入 `DATABASE_URL` 到 `.pytest_cache` 下的独立测试库。
   - 每次测试运行使用唯一测试库文件，避免 Windows 文件锁导致 teardown 删除失败。
   - 结果：后续 `pytest` 不再写入生产 `CaiBao.db`。

2. 生产数据库清理
   - 先创建备份：`CaiBao.db.bak_20260322_151453`。
   - 按要求仅保留账户：`team_id=team1`、`user_id=team1`、`display_name=一只长角的鸟`。
   - 其余团队、用户及无关关联数据（会话/文件/消息及相关配置）已清理。

## 清理后数据状态

1. `teams = 1`
2. `users = 1`
3. `conversations = 8`
4. `documents = 15`
5. `chat_history = 19`

## 验证结果

1. `PYTHONPATH=. pytest -q` 通过：`46 passed`
2. 生产库复核通过：仅剩 `team1/team1/一只长角的鸟`

## 涉及文件

1. `tests/conftest.py`
2. `CaiBao.db`（数据清理）
3. `CaiBao.db.bak_20260322_151453`（清理前备份）
