# v0.6.0 - 2026-03-19

## 版本概览

本版本聚焦三件事：

1. RAG 检索链路正式支持真实 embedding（非 mock）并可安全重建索引。
2. 落地可复现的真实场景评测最小集（A/B/C 共 50 题）。
3. 前端模型策略升级：`default` 走 `.env`，`none` 强制 mock；新增向量模型可配置。

## 新增内容

1. 真实 embedding 配置项与运行时逻辑（provider/base_url/api_key/model/batch）。
2. embedding 模型账号隔离配置接口（按 `team_id + user_id` 管理）。
3. 前端新增“向量模型”下拉与自定义入口。
4. 真实场景评测脚本与语料数据集（Recall@1/3/5、命中来源、失败 Top10、对比报告）。

## 主要变更

1. `EmbeddingService` 升级为双模式：
   `mock`（hashing）与 `real`（调用 `/embeddings`，支持批量、响应校验、错误处理）。
2. 检索链路升级：
   支持批量向量化写入、`rebuild` 重建索引、检索维度一致性校验（不一致返回 400）。
3. 聊天模型默认行为调整：
   前端 `default` 不再走 `echo`，改为走 `/chat/ask` 并读取 `.env`；
   `none` 走 `/chat/ask` 且后端强制 mock。
4. 检索请求支持会话用户选择 embedding 模型：
   `retrieval/index`、`retrieval/search`、`chat/ask` 与历史重编辑链路均可带 `embedding_model`。

## 验证结果

1. Python 回归测试通过：`36 passed`。
2. 前端脚本语法检查通过：`node --check app/web/app.js`。
3. 真实场景评测链路（prepare/eval/compare）已可执行。

## 已实现

1. 真实 embedding 配置项与双模式服务  
   `app/core/config.py`  
   `app/services/embedding_service.py`  
   `.env.example`

2. 检索链路升级（批量索引 + rebuild + 维度校验）  
   `app/services/retrieval_service.py`  
   `app/schemas/retrieval.py`  
   `app/api/routes/retrieval.py`

3. embedding 模型账号隔离配置（列表/新增更新/删除）  
   `app/models/embedding_model_config.py`  
   `app/schemas/embedding_model.py`  
   `app/services/embedding_model_service.py`  
   `app/api/routes/embedding_model.py`  
   `app/api/deps.py`  
   `app/api/router.py`  
   `app/db/session.py`

4. 聊天模型行为重构（default=.env，none=mock）与链路打通  
   `app/web/app.js`  
   `app/web/index.html`  
   `app/schemas/chat.py`  
   `app/api/routes/chat.py`  
   `app/services/rag_chat_service.py`  
   `app/services/llm_service.py`  
   `app/services/llm_model_service.py`

5. 真实场景评测最小集（A/B/C）  
   `docs/rag_simulation/corpus/a_alert_triage.md`  
   `docs/rag_simulation/corpus/a_db_backup_restore.md`  
   `docs/rag_simulation/corpus/a_k8s_capacity_ops.md`  
   `docs/rag_simulation/corpus/a_security_incident_response.md`  
   `docs/rag_simulation/corpus/b_release_gates.md`  
   `docs/rag_simulation/corpus/b_change_calendar.md`  
   `docs/rag_simulation/corpus/b_incident_rollback_flow.md`  
   `docs/rag_simulation/corpus/b_postmortem_and_kb.md`  
   `docs/rag_simulation/corpus/c_api_version_matrix.md`  
   `docs/rag_simulation/corpus/c_model_release_calendar.md`  
   `docs/rag_simulation/corpus/c_policy_effective_dates.md`  
   `docs/rag_simulation/dataset_minimal.json`  
   `docs/rag_simulation/README.md`  
   `scripts/rag_sim_prepare.py`  
   `scripts/rag_sim_eval.py`  
   `scripts/rag_sim_compare.py`  
   `scripts/rag_eval.py`  
   `docs/rag_eval_sample.json`

6. 测试覆盖补充与稳定性增强  
   `tests/test_embedding_service.py`  
   `tests/test_retrieval.py`  
   `tests/test_embedding_model.py`  
   `tests/test_chat.py`  
   `tests/conftest.py`
